public class LinkedList {//    Node class for creating each node in LinkedList;     class Node {        int val;        Node next;        public Node(int val){            this.val = val;            this.next = null;        }    }    Node head = null;    Node ptr = null;    public void addList(int val){        Node newNode = new Node(val);        if(head == null){            head = newNode;            ptr = head;        }        else{            ptr.next = newNode;            ptr = newNode;        }    }    public void displayList(){        if(head == null){            System.out.println("Empty LinkedList");            return;        }else{            Node currentNode = head;            while(currentNode != null){                System.out.print(currentNode.val+" -> ");                currentNode = currentNode.next;            }            System.out.println("null");        }    }    public static void main(String[] args) {        LinkedList list = new LinkedList();        list.displayList();        list.addList(2);        list.addList(3);        list.addList(5);        list.addList(6);        list.addList(9);        list.displayList();    }}